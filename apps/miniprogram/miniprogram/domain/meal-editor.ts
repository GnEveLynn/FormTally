export type Nutrition={energyKcal:number;proteinGrams:number;carbGrams:number;fatGrams:number}
export type MealItem={name:string;grams:number;nutrition:Nutrition;basisPer100Grams?:Nutrition|null}
export type MealDraft={occurredAt:string;mealType:string;items:MealItem[]}
const round1=(value:number)=>Math.round((value+Number.EPSILON)*10)/10
export function scaleNutrition(basis:Nutrition,grams:number):Nutrition{return{energyKcal:Math.round(basis.energyKcal*grams/100),proteinGrams:round1(basis.proteinGrams*grams/100),carbGrams:round1(basis.carbGrams*grams/100),fatGrams:round1(basis.fatGrams*grams/100)}}
export function sumNutrition(values:Nutrition[]):Nutrition{return values.reduce((total,value)=>({energyKcal:total.energyKcal+value.energyKcal,proteinGrams:round1(total.proteinGrams+value.proteinGrams),carbGrams:round1(total.carbGrams+value.carbGrams),fatGrams:round1(total.fatGrams+value.fatGrams)}),{energyKcal:0,proteinGrams:0,carbGrams:0,fatGrams:0})}
export function validateMeal(draft:MealDraft,now=new Date()):Record<string,string>{const errors:Record<string,string>={};const occurred=new Date(draft.occurredAt);if(Number.isNaN(occurred.valueOf())||occurred>now)errors.occurredAt='餐食时间不能位于未来';if(!['breakfast','lunch','dinner','snack'].includes(draft.mealType))errors.mealType='请选择餐别';if(draft.items.length===0)errors.items='至少添加一个食物';draft.items.forEach((item,index)=>{if(!item.name.trim()||item.name.length>60)errors[`items[${index}].name`]='请输入食物名称';if(!Number.isFinite(item.grams)||item.grams<.1||item.grams>10000)errors[`items[${index}].grams`]='重量必须在 0.1 至 10000 克之间';for(const [key,value]of Object.entries(item.nutrition)){if(!Number.isFinite(value)||value<0)errors[`items[${index}].nutrition.${key}`]='营养值必须是非负数'}});return errors}
